name: PHPCS Validation
permissions:
  contents: read
on:
  push:
    paths:
      - "!vendor/**"
      - "!dist/**"
  pull_request:
  workflow_dispatch:
jobs:
  phpcs-validation:
    uses: bcgov/des-git-workflows/.github/workflows/phpcs-validate.yml@main